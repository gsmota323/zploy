# Modo estudo (sem login)
AUTH_ENABLED="false"

# Banco e fila locais (docker compose base)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tcc_db"
REDIS_HOST="127.0.0.1"
REDIS_PORT="6379"

# Ainda usado por algumas rotas/processos
JWT_SECRET="zploy-secret"
GITHUB_WEBHOOK_SECRET=""

# Usuario local automatico (modo estudo)
LOCAL_DEV_EMAIL="study@zploy.local"
LOCAL_DEV_PASSWORD="study123"
